<div class="card">
  <div class="card-header bg-primary text-white">
    <h5 class="mb-0">💸 Lista de Gastos</h5>
  </div>
  <div class="card-body p-0">
    <table class="table table-hover table-striped mb-0">
      <thead class="table-dark">
        <tr>
          <th>#</th>
          <th>Descripción</th>
          <th>Monto</th>
          <th>Categoría</th>
          <th>Fecha</th>
        </tr>
      </thead>
      <tbody>
        <% if (gastos.length === 0) { %>
          <tr>
            <td colspan="5" class="text-center text-muted py-3">No hay gastos registrados.</td>
          </tr>
        <% } else { %>
          <% gastos.forEach((g, index) => { %>
            <tr>
              <td><%= index + 1 %></td>
              <td><%= g.Descripcion %></td>
<td>$<%= g.Monto ? Number(g.Monto).toFixed(2) : '0.00' %></td>
              <td><span class="badge bg-secondary"><%= g.categoria?.Nombre %></span></td>
<td><%= new Date(g.FechaGasto).toLocaleDateString('es-CO') %></td>
            </tr>
          <% }) %>
        <% } %>
      </tbody>
    </table>
  </div>
</div>

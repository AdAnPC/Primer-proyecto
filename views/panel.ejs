<div class="panel-container">
  <div class="cards-row">
    <div class="card blue">
      <h4>Usuarios</h4>
      <p><%= totalUsuarios %></p>
    </div>
    <div class="card cyan">
      <h4>Ingresos</h4>
      <p>$<%= ingresosTotales.toFixed(2) %></p>
    </div>
    <div class="card green">
      <h4>Ganancias</h4>
      <p>$<%= totalGanancias.toFixed(2) %></p>
    </div>
    <div class="card yellow">
      <h4>Tasa conversión</h4>
      <p><%= tasaConversion %>%</p>
    </div>
    <div class="card red">
      <h4>Sesiones</h4>
      <p><%= totalSesiones %></p>
    </div>
  </div>

  <div class="graph-section">
    <h4>Ventas por Mes</h4>
    <canvas id="ventasChart" width="600" height="300"></canvas>
  </div>
</div>

<!-- Variables JS inyectadas desde EJS -->
<script>
  const labels = <%- JSON.stringify(labels) %>;
  const dataVentas = <%- JSON.stringify(dataVentas) %>;
</script>
